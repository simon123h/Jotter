[tool.ruff]
line-length = 140
target-version = "py312"

[tool.ruff.lint]
# Enable Pyflakes (F), pycodestyle (E, W), isort (I), and bugbear (B) rules.
select = ["E", "F", "I", "B", "W"]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
