{
  "name": "jotter",
  "version": "1.0.0",
  "private": true,
  "description": "Jotter - Local-First Markdown Kanban App with Ephemeral Index",
  "scripts": {
    "dev:desktop": "wails dev",
    "dev:backend": "go run main_server.go shared_assets.go",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently --kill-others-on-fail --names \"Backend,Frontend\" --prefix-colors \"blue,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:desktop": "wails build",
    "build:server": "cd frontend && npm run build && cd .. && go build -o build/bin/jotter-server main_server.go shared_assets.go",
    "build": "npm run build:desktop && npm run build:server",
    "test:backend": "go test -v ./internal/... ./tests/...",
    "test:frontend": "cd frontend && npm run test",
    "test": "npm run test:backend && npm run test:frontend",
    "test:coverage": "go test -coverprofile=coverage.out -coverpkg=./internal/... ./internal/storage ./internal/config ./internal/db ./internal/app ./tests/... && cd frontend && npm run test:coverage && cd .. && node scripts/combine-coverage.js",
    "lint:backend": "go vet ./...",
    "lint:backend:fix": "echo \"No automatic linter fix for Go backend\"",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:frontend:fix": "cd frontend && npm run lint:fix",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:fix": "npm run lint:backend:fix && npm run lint:frontend:fix",
    "format:backend": "go fmt ./...",
    "format:backend:check": "test -z \"$(gofmt -l . | grep -v 'node_modules\\|build\\|frontend/node_modules')\"",
    "format:frontend": "cd frontend && npm run format",
    "format:frontend:check": "cd frontend && npm run format:check",
    "format": "npm run format:backend && npm run format:frontend",
    "format:check": "npm run format:backend:check && npm run format:frontend:check",
    "install:all": "npm install && cd frontend && npm install && go mod download"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
