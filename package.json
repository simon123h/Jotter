{
  "name": "jotter",
  "version": "1.0.0",
  "private": true,
  "description": "Jotter - Local-First Markdown Kanban Board",
  "scripts": {
    "dev:backend": "python3 run.py --reload --no-browser",
    "dev:frontend": "cd frontend && npm run dev",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "dev": "concurrently --kill-others-on-fail --names \"Backend,Frontend\" --prefix-colors \"blue,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "cd frontend && npm run build",
    "build:apk": "cd frontend && npm run build && npx cap sync android && cd android && ./gradlew assembleRelease",
    "build:package": "npm run build:frontend && rm -rf src/jotter/dist build && cp -r frontend/dist src/jotter/dist && pyproject-build",
    "build": "npm run build:package",
    "test:backend": "python3 -m pytest tests/",
    "test:frontend": "cd frontend && npm run test",
    "test": "npm run test:backend && npm run test:frontend",
    "lint:backend": "ruff check .",
    "lint:backend:fix": "ruff check --fix .",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:frontend:fix": "cd frontend && npm run lint:fix",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:fix": "npm run lint:backend:fix && npm run lint:frontend:fix",
    "format:backend": "ruff format .",
    "format:backend:check": "ruff format --check .",
    "format:frontend": "cd frontend && npm run format",
    "format:frontend:check": "cd frontend && npm run format:check",
    "format": "npm run format:backend && npm run format:frontend",
    "format:check": "npm run format:backend:check && npm run format:frontend:check",
    "install:all": "npm install && cd frontend && npm install && cd .. && pip install -e .[dev]",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "vitepress": "^1.6.4"
  },
  "lint-staged": {
    "*.py": [
      "ruff check --fix",
      "ruff format"
    ],
    "frontend/src/**/*.{js,ts,vue,css}": [
      "npm --prefix frontend run format",
      "npm --prefix frontend run lint:fix"
    ]
  },
  "overrides": {
    "vite": "^6.4.2",
    "esbuild": "^0.28.1"
  }
}
